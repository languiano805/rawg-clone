import { Button } from "@chakra-ui/react";
import React from "react";

const App = () => {
  return (
    <div>
      <Button colorScheme="teal" size="lg">
        Button
      </Button>
    </div>
  );
};

export default App;
